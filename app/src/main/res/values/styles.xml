<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="myButton">
        <item name="android:textColor">#000</item>
        <item name="android:background">@drawable/roundbtn</item>
        <item name="android:layout_width">135dp</item>
        <item name="android:textAllCaps">false</item>
    </style>
    <style name="mySmallerButton" parent="myButton">
        <item name="android:layout_width">85dp</item>
        <item name="android:radius">10dp</item>
    </style>

    <style name="headerText">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">#000</item>
        <item name="android:typeface">serif</item>
    </style>

    <style name="smallerHeaderText" parent="headerText">
        <item name="android:textSize">20sp</item>
        <item name="android:typeface">normal</item>
    </style>

    <style name="normalText" parent="smallerHeaderText">
        <item name="android:textSize">16sp</item>
    </style>

</resources>
